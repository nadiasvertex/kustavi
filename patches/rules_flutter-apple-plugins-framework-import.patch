diff --git flutter/private/flutter_plugin.bzl flutter/private/flutter_plugin.bzl
index e169c1d..61039fd 100644
--- flutter/private/flutter_plugin.bzl
+++ flutter/private/flutter_plugin.bzl
@@ -17,6 +17,8 @@ Platform-specific native dependencies use select() in BUILD files:
     )
 """
 
+load("@rules_apple//apple:aspects.bzl", "framework_provider_aspect")
+load("@rules_apple//apple:providers.bzl", "AppleFrameworkImportInfo")
 load("@rules_cc//cc/common:cc_info.bzl", "CcInfo")
 load("@rules_dart//dart:providers.bzl", "DartCodeAssetInfo", "DartInfo")
 load("@rules_dart//dart:utils.bzl", "derive_lib_root", "derive_package_name")
@@ -87,6 +89,7 @@ def _flutter_plugin_impl(ctx):
             label = lib.label,
             cc_info = lib[CcInfo] if CcInfo in lib else None,
             swift_info = lib[SwiftInfo] if SwiftInfo in lib else None,
+            framework_import_info = lib[AppleFrameworkImportInfo] if AppleFrameworkImportInfo in lib else None,
             package = package_name,
         ))
 
@@ -237,6 +240,15 @@ flutter_plugin = rule(
         "apple_libs": attr.label_list(
             doc = "Per-platform Apple swift_library targets (typically `flutter_apple_plugin_library`s) carrying the plugin's macOS / iOS native code. Each label's name must contain `macos` or `ios` so the runner aggregator can route it. Set this via `select()` on the target platform so only one platform's swift_library analyzes per build — both with the same module name in the same configuration would conflict on the produced `<module>.swiftmodule` output.",
             providers = [CcInfo],
+            # Lets a plugin's `AppleFrameworkImportInfo` (e.g. a vendored
+            # native SDK the plugin's ObjC/C++ sources link against, such as
+            # fvp's mdk-sdk) reach the runner aggregator below — otherwise
+            # rules_apple's own framework auto-embedding aspect, which walks
+            # `deps`/`data`/`frameworks`/etc., never sees past this
+            # label_list attribute (a custom rules_flutter name it doesn't
+            # know about) and the framework compiles+links fine but never
+            # gets copied into the app bundle.
+            aspects = [framework_provider_aspect],
         ),
         "linux_libs": attr.label_list(
             doc = "Linux plugin source bundles (`flutter_linux_plugin_library`). Propagated through `FlutterInfo.linux_plugin_libraries` so the Linux runner folds them into its `cc_common.compile()` pass.",
diff --git flutter/private/flutter_apple_plugins_aggregator.bzl flutter/private/flutter_apple_plugins_aggregator.bzl
index 1673c69..727a786 100644
--- flutter/private/flutter_apple_plugins_aggregator.bzl
+++ flutter/private/flutter_apple_plugins_aggregator.bzl
@@ -16,6 +16,7 @@ rule walks that depset, filters to the target platform, and merges:
 The runner_lib_gen swift_library has the aggregator target in its `deps`.
 """
 
+load("@rules_apple//apple:providers.bzl", "apple_provider")
 load("@rules_cc//cc/common:cc_common.bzl", "cc_common")
 load("@rules_swift//swift:swift.bzl", "SwiftInfo")
 load("//flutter:providers.bzl", "FlutterInfo")
@@ -26,6 +27,7 @@ def _flutter_apple_plugins_aggregator_impl(ctx):
 
     cc_infos = []
     swift_infos = []
+    framework_import_infos = []
     for entry in flutter_info.apple_plugin_libraries.to_list():
         if entry.platform != target_platform:
             continue
@@ -33,6 +35,8 @@ def _flutter_apple_plugins_aggregator_impl(ctx):
             cc_infos.append(entry.cc_info)
         if entry.swift_info:
             swift_infos.append(entry.swift_info)
+        if entry.framework_import_info:
+            framework_import_infos.append(entry.framework_import_info)
 
     # Always emit CcInfo + SwiftInfo, even when there are no plugins.
     # The runner's swift_library depends on this aggregator
@@ -42,12 +46,29 @@ def _flutter_apple_plugins_aggregator_impl(ctx):
     # e2e/macos_example) still needs an aggregator that satisfies the
     # contract. cc_common.merge_cc_infos and SwiftInfo handle empty lists
     # cleanly and produce no-op merged providers.
-    return [
+    providers = [
         DefaultInfo(),
         cc_common.merge_cc_infos(cc_infos = cc_infos),
         SwiftInfo(swift_infos = swift_infos),
     ]
 
+    # A plugin's own AppleFrameworkImportInfo (e.g. a vendored native SDK
+    # its ObjC/C++ sources link against — fvp's mdk-sdk, say) needs to
+    # reach here so the framework actually gets embedded in the app
+    # bundle. rules_apple's own framework_provider_aspect walks
+    # deps/data/frameworks/etc., but this aggregator's relevant attribute
+    # is `application` (a whole flutter_application, carrying plugin data
+    # through FlutterInfo) — not one the aspect's attr_aspects list
+    # knows about, so it never reaches past this node on its own.
+    # Exposing the already-merged provider here natively is exactly what
+    # framework_provider_aspect expects to find on a node it can't fully
+    # see through (its impl explicitly skips recomputing when a visited
+    # target already has AppleFrameworkImportInfo).
+    if framework_import_infos:
+        providers.append(apple_provider.merge_apple_framework_import_info(framework_import_infos))
+
+    return providers
+
 flutter_apple_plugins_aggregator = rule(
     implementation = _flutter_apple_plugins_aggregator_impl,
     attrs = {
