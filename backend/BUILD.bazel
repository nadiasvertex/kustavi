load("@rules_cc//cc:defs.bzl", "cc_binary")

cc_binary(
    name = "server",
    srcs = glob([
        "src/**/*.cpp",
        "include/**/*.h",
    ]),
    includes = ["include"],
    deps = [
        "//proto:service_cc_grpc",
        "@opencv//:core",
        "@opencv//:imgproc",
        "@opencv//:imgcodecs",
        "@opencv//:objdetect",
        "@sqlite3//:sqlite3",
        "@cli11",
        "@spdlog",
    ],
    visibility = ["//visibility:public"],
)
